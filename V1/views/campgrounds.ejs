<!-- Header Partial -->
<% include partials/header %>

<h1>This is the Campgrounds Page</h1>

<% campgrounds.forEach(function(campground){ %>
    <div>
        <h4><%= campground.name %></h4>
        <img src="<%= campground.image %>" width="65%">
    </div>
<% }); %>

<!-- Footer Partial -->
<% include partials/footer %>